# Development / test dependencies. Install on top of requirements.txt:
#     pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest==9.0.3              # CVE-2025-71176 (tmpdir handling)
python-docx==1.2.0         # generates DOCX fixtures for the test suite
